{
  "_note": "Production environment configuration for ICP + Vercel deployment. Values marked with VITE_ are injected at build time by Vite from Vercel Environment Variables or local .env files.",

  "backend_host": "https://icp-api.io",

  "backend_canister_id": "${VITE_CANISTER_ID_BACKEND}",

  "dfx_network": "${VITE_DFX_NETWORK}",

  "project_id": "dr-arman-kabir-s-care",

  "ii_url": "https://identity.internetcomputer.org",

  "ii_derivation_origin": "${VITE_II_DERIVATION_ORIGIN}",

  "frontend_origin": "${VITE_FRONTEND_ORIGIN}"
}
